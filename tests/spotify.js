var should = require("should");

describe('Spotify'function() {
    describe('When I search for a track', function(){
        it('should return the response of the Spotify API', function (done){
            var parseResponseCallback = function (res){
                console.log(res);
            };
        });
    });
});